Continuing the affiliation cleanup, tenth session. Read CLAUDE.md — "Where this is up to" is
the current state of the build and is accurate. NEXT_STEPS.md is the work queue: steps 1 to 7
are done, and step 8 (Gia's lake-region data) is answered and built, with its closing runs
left. Do not read anything else to get oriented, and do not re-audit the project.

FIRST ACTION: read those two files and run nothing. Put the two questions under STEP 8 below
to me, then wait: I will tell you when to do the closing runs.

STATE, 2026-09-17, 15:30. I answered both step-8 sheets and the loop was run; the last
rebuild wrote the deliverables at 15:30. The ninth session checked the files read-only and ran
nothing, so verify is NOT confirmed on this build.
  label_review.xlsx   255 rows, none open. Step 8's 51 pairs: 21 accepted (20 label_rename
                      rows), 30 rejected. IHI Ifakara Tanzania and Ifakara Centre Ifakara
                      Tanzania both went into IHI Ifakara.
  coord_review.xlsx   241 rows, every label settled. 31 new coordinate rows: 22 of Gia's
                      points ticked as they stand, 6 conflicts the merges raised, ILRAD given
                      the ILRI point, my own points for ICIPE Nairobi Kenya and MasenoU.
  decisions           312, all `applied` in output/decisions_report.csv (168 coordinate,
                      132 label_rename, 7 accept_as_is, 5 affiliation_relabel).
  deliverables        affiliations_complete.csv 1640 rows, 746 papers; affiliation_lookup.csv
                      1290 pairs; affiliation_simple_coords.csv 327 labels: 168 decided,
                      156 ok, 3 absent, 0 missing, 0 conflict. No coordinate still comes
                      unchecked from Gia's counts file.

STEP 8 — CLOSING RUNS, when I say go (lock files first). Commands at the top of NEXT_STEPS.md.
  1. python3 R/verify_affiliations.py — 49 checks expected.
  2. check_label_candidates.R and check_coord_sanity.R. Both last ran at 12:33, before the
     merges and the new coordinates, so "109 pairs" and "0 flagged" are the old build's.
  3. label_review.R, then coord_review.R, so anything the sweeps raise reaches the sheets.
     Housekeeping too: the 21 accepted rows still read `accepted`, and the 23 ticked lake
     rows should turn `manual`. Rebuild and verify if a decision changes.
  4. The rebuild should no longer print the `!! lake row 50117` notice; say if it does.
  5. output/final/README.txt still has the pre-answer counts (347 labels, 1298 pairs);
     update it once the runs are done.

TWO ANSWERS TO PUT TO ME — facts, no recommendation; they are my calls:
  - MTTI Kendu Bay Kenya ("Mawego Technical Training Institute, Kendu Bay, Kenya") was
    ticked at Gia's point as it stood, -4.0435, 39.6682, which is Mombasa. Of the four
    far-from-town points step 8 listed, it is the only one left where it was.
  - KU Kenya and KenyattaU Nairobi Kenya were merged into JKUAT Kenya, which now holds 10
    Kenyatta University affiliation strings (P.O. Box 43844, Nairobi) beside 5 for Jomo
    Kenyatta University of Agriculture and Technology. Kenyatta University (Kahawa, Nairobi)
    and JKUAT (Juja) are separate universities.

Also fixed on 2026-09-17, recorded in CLAUDE.md and step 5: R/absent_review.R re-appended 66
answers on every run, so deliverable 1 had 1374 rows instead of 1308 and verify passed it.
The script now owns every paper in its sheet; verify's 43rd check compares the additions
file with the sheet.

HOW WE WORK. There are four sheet-and-script loops and I make every judgement call in a
sheet. You never put an answer in a sheet for me — not yes/no, not a coordinate, not a
label. You run the scripts, rebuild, verify, and report.

  R/coord_review.R    data/coord_review.xlsx    coordinates
  R/label_review.R    data/label_review.xlsx    label merges
  R/absent_review.R   data/absent_review.xlsx   ABSENT affiliations
  R/check_coord_sanity.R and R/check_label_candidates.R are read-only sweeps that feed
  the first two.

What each sheet takes:
  coord_review.xlsx   column A: yes / no / absent. yes takes the coordinate in H and I;
                      `absent` leaves the label without one and appends an accept_as_is
                      decision carrying my note from M. Six cells are read: A, C, H, I, M, N.
  label_review.xlsx   column A: yes / no. Column C `keep` names the survivor and may name
                      any label — or `new: <name>` to create one. Four cells are read: A, C,
                      R, S. `warning` is Q, the two maps links W and X, `lake` Y.

Run label_review.R before coord_review.R when both are in play: a merge patches the
coordinate sheet and coord_review.R turns that patch back into decisions.

NEVER overwrite data/affiliation_decisions.csv — only add to it. It is the record of every
change. The only rows ever rewritten are the `coordinate` and `label_rename` rows, which
their own scripts regenerate from their sheets; coord_review.R also appends accept_as_is
rows for labels I leave without a coordinate, and never rewrites those. That is rule 2 in
CLAUDE.md.

Never hand me a manual procedure for work that recurs. If a task needs the same judgement
made repeatedly and no mechanism exists, say so plainly and build the loop. Check the work
actually surfaces where I am working: step 6 existed because 12 labels never did, and the
`case = lake` rows exist because Gia's points otherwise never would.

Test a change to any script on a sandbox copy before running it on my files, and say what
the test showed. The sandbox recipe is in memory; include data/gia_final_data. Do NOT copy
or symlink data/vector_extraction_data.csv into it.

If verify fails after a change that legitimately moves a count, teach the assertion the new
expectation, computed from the same inputs. Do not loosen or delete it, and do not compute
the expectation the same way the pipeline does — or take it from a file the pipeline
generates: that is how 66 duplicate rows passed verify. It runs 36 checks with an empty
decisions file, 41 with decisions, 43 once added_affiliations.csv holds rows, 49 once
data/gia_final_data/ is present.

WHEN I ASK ABOUT A LABEL. Answer from the review CSVs in output/ plus a web search if the
institution is unclear. Do not volunteer opinions on labels I have not asked about — EXCEPT
that when I name a label to merge or rename, list every other label naming the same
institution, place or acronym, with row counts, coordinates and coord_status, as a short
block with no recommendation. Typos, institution identity and coordinate choices are my
calls: surface them, do not settle them. If I write a label name that looks like a slip, ask
before writing it into a deliverable.

TRAPS THAT HAVE COST TIME.

- The scripts rewrite their own xlsx on every run. If the sheet is still open in Excel the
  script reads the on-disk version without my unsaved edit and then overwrites the file —
  the edit vanishes silently. I often have a sheet open while I am talking to you, so check
  for the lock file data/~$<sheet>.xlsx before running, and stop if it is there. Remind me:
  type, save, close, run.
- The build goes stale quietly. `*_review.R` writes decisions; nothing is in the
  deliverables until `tidy_affiliations.py` runs.
- After a batch of merges the coordinate sheet needs two passes: coord_review.R reads the
  current deliverables, so run it, rebuild, then run it again. A merged-away label's lake row
  goes on the second pass, and a merge of an ok label into one of Gia's with a different
  point shows up there as a conflict with both points open.
- A ticked sanity or lake row turns `case = manual` once its source stops offering it. That
  is expected; its decision and its recorded note are kept.
- Rebuilds printed `!! lake row 50117 keeps her label 'IHI Ifakara Tanzania' ...` until that
  label was merged into IHI Ifakara, which I did on 2026-09-17. It should now be gone; if it
  still prints, say so.
- A sweep saying "0 flagged" only covers what it can see. check_coord_sanity.R tests only
  the country, so a point in the wrong town passes — which is exactly the state of several
  of Gia's points.
- output/final/ holds the deliverables. output/parity_baseline/ is a frozen pre-decisions
  build kept only as a test fixture and is NOT the product. Both have a README.txt.
- The `decided_on` dates on label_rename rows are immaterial and settled; do not raise them.
- label_review.R writes the label_rename rows in its own order. On 2026-09-17 that moved the
  two DRASS Reunion renames within the decisions file, once; they now sit where the script
  writes them. Same rows; not a change to raise.

NEVER READ, they are huge and irrelevant: data/vector_extraction_data.csv — not once, not
for any purpose, no sanctioned exception — output/diff_report_20260817.csv,
output/review_encoding_repairs.csv, output/review_simple_lumping.csv, and any
.tif/.geotiff/.zip. Use grep or head on the review CSVs; never cat one whole.

STILL OPEN, LOWER PRIORITY, do not start unless I ask:
- output/review_label_lumping.csv (101) and output/review_simple_lumping.csv (157): how
  coarse a label should be. `SAP Italy` covers 45 affiliation strings, `OCEAC Cameroon` 28.
  Deliberately not pursued.
- National Center for the Fight against Malaria, Ouagadougou, Burkina Faso (1 row, 4
  records, 12.3642904, -1.5050645) and CNRFP Burkina Faso (15 rows, 822 records, 12.349733,
  -1.4903285) are 2.3 km apart and deliberately left as two labels; the sweep does not
  raise them. Do not re-raise it unless I do.
- Noticed 2026-09-17, cosmetic, not fixed: output/review_labels_collapsed.csv comes out in a
  different row order on each run (Python set order), and absent_review.R reports
  `OCCGE Benin` as a new label wanting a coordinate on every run although a rename folds it
  into CREC Benin.
