Continuing the affiliation cleanup, fifth session. Read CLAUDE.md — the "Where this is up
to" section is the current state and is accurate as of the end of the last session. Read
NEXT_STEPS.md for the work: it runs top to bottom, steps 1 to 5, and everything after the
Reference heading is background. Do not read anything else to get oriented, and do not
re-audit the project.

STATE. The build is clean: 233 decisions, all applying, and `python3
R/verify_affiliations.py` ends `41 checks, 0 failed`. The three deliverables are in
output/final/ (1278 rows, 986 lookup pairs, 298 labels — 169 ok, 116 decided, 13 absent,
nothing missing or in conflict), with a README.txt there documenting every column. Steps 1
to 4 are done: labels merged, every coordinate settled, the coordinate sanity sweep flags
0 of 284, spot-checks reviewed, and the one paper version 3 never covered has its five
affiliations in.

FIRST ACTION: run `python3 R/tidy_affiliations.py && python3 R/verify_affiliations.py` and
tell me the counts. Expect 41 checks, 0 failed. Then wait.

WHAT IS OPEN — step 5, and nothing else. 59 rows of deliverable 1 carry `affiliation =
ABSENT`, over 58 papers: whoever entered them could not find an affiliation. They are in
data/absent_review.xlsx, all open, one row per ABSENT row. I type the affiliation into
column A and its label into column B. 55 of the papers are ABSENT and nothing else; 3 have
real affiliations alongside a placeholder, and their existing labels show in
`other_labels_on_this_paper`; 2 rows arrive with column B already filled because the row
had a label but no affiliation text — leave those labels alone unless they are wrong.

Loop, every time: `Rscript R/absent_review.R` → I edit the sheet → `Rscript
R/absent_review.R` → `python3 R/tidy_affiliations.py && python3
R/verify_affiliations.py`. Report the counts and anything that did not apply. A new label
in column B creates a new label, which then wants a coordinate and appears in
coord_review.xlsx as a `needs a coordinate` row — tell me when that happens.

HOW WE WORK. There are four sheet-and-script loops and I make every judgement call in a
sheet. You never put an answer in a sheet for me — not yes/no, not a coordinate, not a
label. You run the scripts, rebuild, verify, and report.

  R/absent_review.R   data/absent_review.xlsx   ABSENT affiliations   <- the live one
  R/coord_review.R    data/coord_review.xlsx    coordinates
  R/label_review.R    data/label_review.xlsx    label merges
  R/check_coord_sanity.R and R/check_label_candidates.R are read-only sweeps that feed
  the first two.

Run label_review.R before coord_review.R when both are in play: a merge patches the
coordinate sheet and coord_review.R turns that patch back into decisions.

Never hand me a manual procedure for work that recurs. If a task needs the same judgement
made repeatedly and no mechanism exists, say so plainly and build the loop — that is what
the four scripts are. Check the work actually surfaces where I am working; twice I have
been told to act on something that had no way of reaching me.

If verify fails after a change that legitimately moves a count, teach the assertion the new
expectation, computed from the same inputs. Do not loosen or delete it. See the section in
CLAUDE.md.

WHEN I ASK ABOUT A LABEL. Answer from the review CSVs in output/ plus a web search if the
institution is unclear. Do not volunteer opinions on labels I have not asked about —
EXCEPT that when I name a label to merge or rename, list every other label naming the same
institution, place or acronym, with row counts, coordinates and coord_status, as a short
block with no recommendation. Typos, institution identity and coordinate choices are my
calls: surface them, do not settle them. If I write a label name that looks like a slip,
ask before writing it into a deliverable.

TRAPS THAT HAVE COST TIME.

- The scripts rewrite their own xlsx on every run. If the sheet is still open in Excel the
  script reads the on-disk version without my unsaved edit and then overwrites the file —
  the edit vanishes silently. Remind me: type, save, close, run. This lost a coordinate on
  2026-09-10.
- The build goes stale quietly. `*_review.R` writes decisions; nothing is in the
  deliverables until `tidy_affiliations.py` runs. Twice a "failure" was just a missing
  rebuild.
- After a batch of merges the coordinate sheet can need two passes: coord_review.R reads
  the current deliverables, so run it, rebuild, then run it again if stale rows remain.
- output/final/ holds the deliverables. output/parity_baseline/ is a frozen pre-decisions
  build kept only as a test fixture — it still shows 88 missing and 29 conflict and is NOT
  the product. It was called output/shipped/ until 2026-09-10, and that name caused exactly
  the confusion you would expect. Both directories have a README.txt.

NEVER READ, they are huge and irrelevant: data/vector_extraction_data.csv,
output/diff_report_*.csv, output/review_encoding_repairs.csv,
output/review_simple_lumping.csv, and any .tif/.geotiff/.zip. Use grep or head on the
review CSVs; never cat one whole.

STILL OPEN, LOWER PRIORITY, do not start unless I ask:
- output/review_label_lumping.csv (84) and output/review_simple_lumping.csv (124): how
  coarse a label should be. `SAP Italy` covers 45 affiliation strings, `OCEAC Cameroon` 28.
  Deliberately not pursued.
- The deliverable filenames still carry 20260817, the date the automated repair ran, not
  the judgement calls since. Kept deliberately so downstream references still resolve.
